export enum SortField {
  LATEST = 'latest',
  POPULAR = 'popular',

  // 手动筛选 非接口返回
  VOTED = 'voted',
  DEFAULT = 'default',
}

export enum WORK_TYPE {
  PREMIUM = 'premium',
  MOBOX = 'mobox',
  LATEST = 'latest',
  DEFAULT = 'default',
}
